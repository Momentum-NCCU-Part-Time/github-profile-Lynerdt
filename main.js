const gitAPI = "https://api,github.com/users/lynerdt";
const reposiAPI = "https://api.github.com/users/lynerdt/repos";
let profile = document.getElementById("profile");
let profileinfo = document.getElementById("profile-info");

fetch(gitAPI).then((repsonse) => {
  return Response.json();
});
